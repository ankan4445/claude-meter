# claude-meter
A Claude Code plugin that tracks token usage, cost, time, and activity per session. It provides a `/session-manager` skill and a `Stop` hook that accumulates token metrics after every turn.
